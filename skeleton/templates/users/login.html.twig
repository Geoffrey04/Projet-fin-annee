{% extends 'base.html.twig' %}

{% block body %}


    <form action="{{ path('security_connexion') }}" method="post">

        {% if app.user %}
        {% if error %}
            <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}
        {% endif %}


        <div class="form-group">
            <input type="text" placeholder="pseudo" required name="_username">
        </div>
        <div class="form-group">
            <input type="password" placeholder="mot de passe" required name="_password">
        </div>
        <div class="form-group">
            <button type="submit">Envoyez</button>
        </div>
    </form>
{#{% if app.user %}
{% if app.user.id is same as (10) %}
        <p>Bonjour je suis un admin</p>
    {% endif %}
    {% endif %}  #}



{% endblock %}