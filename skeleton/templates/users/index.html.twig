{% extends 'base.html.twig' %}

{% block title %}Hello UsersController!{% endblock %}

{% block body %}
{#<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/Users/Geoffrey/PhpstormProjects/ProjetPerso_n-3/skeleton/src/Controller/UsersController.php'|file_link(0) }}">src/Controller/UsersController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/Users/Geoffrey/PhpstormProjects/ProjetPerso_n-3/skeleton/templates/users/index.html.twig'|file_link(0) }}">templates/users/index.html.twig</a></code></li>
    </ul>
</div>#}
    <div class="search_profiles" style="border: 1px solid black;">
        <h2 id="title_r">Recherche</h2>


        <input type="search"  name="q" id="search_name" onkeyup="search_name()"
               aria-label="Search through site content" placeholder="nom/pseudo">
        <input type="search"  name="q" id="search_style"
               aria-label="Search through site content" placeholder="Style(Rock, Jazz , etc ...">
        <input type="search"  name="q" id="search_influence"
               aria-label="Search through site content" placeholder="Influence(Chanteur, groupe, etc...">
        <button type="submit">Chercher</button>


    </div>

    <table class="table">
        <thead>
            <tr>
                <th>Avatar/Photo.profil</th>
                <th>Nom/pseudo</th>
                <th>Ville</th>
                <th>Age</th>
                <th>Styles</th>
                <th>influences(artistes,groupes)</th>


            </tr>
        </thead>

        <tbody>
        {% for user in users %}
            <tr id="Mytr" style="">
                <td><img class="img_avatar" src="{{ user.avatar }}"></td>
                <td class="js-search-nom"><a href="#">{{ user.username }}</a></td>
                <td>{{ user.city }}</td>
                <td>{{ user.age }}</td>
                <td>{{ user.styles }}</td>
                <td>{{ user.influences }}</td>
                {% endfor %}
            </tr>
        </tbody>

    </table>

{% endblock %}
{% block javascripts %}
    <script  src="{{ asset('js/app.js') }}"></script>
{% endblock %}
