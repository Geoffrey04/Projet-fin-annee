<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <meta content="width=device-width, initial-scale=1" name="viewport"/>
    {% block stylesheets %}<link rel="stylesheet" type="text/css" href="{{ asset('css/style.css') }}">
        <link rel="stylesheet" type="text/css" href="../../js/lightbox2/dist/css/lightbox.css"> {% endblock %}
</head>
<body>

<header>
    <h1>Drums & Co</h1>
    <div class="account_manager">
        <div class="link_account">


            {% if app.user %}
        <a href="{{ path('logout_user') }}">Déconnexion</a>
                {% else %}
                    <a href="{{ path('security_connexion') }}">Connexion</a>


                    <a href="{{ path('registration_user') }}">Inscription</a>
            {% endif %}

        </div>
    </div>

    <nav>
        <!--    <span>| | |</span>   ( menu hamburger -> + tard ) -->

        <a href="{{ path('parts_index') }}" class="main_link">Accueil</a>

                <div class="Parts_secondaryMenu">
                <a href="" class="main_link">Morceaux</a>
                    <div class="under_category">
                        {% if app.user %}
                        <a href="{{ path('parts_new') }}">Ajouter</a>
                        <a href="{{ path('parts_search') }}">Rechercher</a>
                        {% endif %}
                    </div>
                </div>



                <div class="Profile_secondaryMenu">
                <a href="" class="main_link">Profil</a>
                    <div class="under_category">
                        {% if app.user %}
                        <a href="{{ path('edit_urprofile', {'id' : user.id}) }}">Editer</a>
                        <a href="{{ path('user_profile', {'id' : user.id}) }}">Voir</a>
                        {% endif %}
                    </div>
                </div>


            <a href="{{ path('users_search') }}" class="main_link">Chercher Membres</a>

    </nav>
</header>


{% block body %}
{% endblock %}

<footer>
    <p>Some contains in footer of this page like blablabla</p>
</footer>

{% block javascripts %}
    <script  src="{{ asset('js/app.js') }}"></script>
    <script src="../../js/lightbox2/dist/js/lightbox-plus-jquery.js" type="text/javascript"></script>
{% endblock %}
</body>
</html>
